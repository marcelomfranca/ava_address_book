import 'package:flutter/material.dart';

abstract interface class StylesAVA {
  static const textFormField = TextStyle(fontSize: 16, height: 1.1);
  static const listTileDrawer = TextStyle(fontSize: 15);
  static const validateErrorText = TextStyle(fontSize: 12, color: Colors.red);
  static const infoTextField = TextStyle(fontSize: 12, color: Colors.black);
}
